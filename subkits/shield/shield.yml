---
instance_groups:
- name: jumpbox
  jobs:
  - name: shield-agent
    release: shield

properties:
  daemon_public_key: (( vault params.shield_public_key_vault_path ))
  shield:
    agent:
      daemon_public_key: (( vault params.shield_public_key_vault_path ))

releases:
- name: shield
  version: latest
